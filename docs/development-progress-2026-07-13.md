# Stock Doctor 开发进度交接 - 2026-07-13

## 当前分支

- 本地/远端开发分支：`local-codex-progress`
- 远端仓库：`https://github.com/kekeyang550/stock-doctor`
- 本机后端运行：`http://127.0.0.1:8010`
- 本机前端运行：`http://127.0.0.1:30080`

## 本轮已完成

1. 真实数据接入继续增强
   - 新增 `EastmoneyMarketDataProvider`，主链路优先直连东方财富 A 股行情、指数、日 K 线、单股估值详情和资金流。
   - 东方财富报价/K 线不可用时，自动尝试腾讯行情备用源。
   - 东方财富资金流不可用时，新增新浪资金流备用源，诊断资金面不再轻易退回样例值。
   - 本机验证 `600036` 招商银行可通过真实链路生成诊断，主力资金流可显示到证据链。

2. 本地行情参考源
   - 新增通达信本地日线读取器，默认读取 `E:\new_tdx64\vipdoc`。
   - 主行情源可用时做最近收盘价交叉校验；网络 K 线失败时可作为本地兜底。
   - 新增同花顺本地股票名表读取器，默认读取 `D:\同花顺软件\同花顺\stockname\stockname_16_0.txt` 和 `stockname_32_0.txt`。
   - 搜索股票名时可通过同花顺本地名表解析代码，例如“招商银行”解析到 `600036`。

3. 任意 A 股搜索、加入和诊断
   - 搜索框支持 6 位 A 股代码直连查询。
   - 不在默认候选池中的股票也可加入自选并生成诊断。
   - 搜索结果保留 `diagnosable`、数据质量状态和匹配原因。
   - 2026-07-15 新增常见市场代码格式归一化，支持 `600519.SH`、`SH600519`、`sz000001`、`000001-SZ` 等输入；搜索、加入自选、移出自选、诊断、东方财富/腾讯行情、Tushare 和通达信本地日线都会统一转换为 6 位 A 股代码。
   - 旧状态文件里如果已保存过带市场前后缀的自选股，读取自选股时会自动归一化并去重，避免同一只股票因格式不同重复或无法诊断。
   - 2026-07-15 继续把笔记、价位提醒和 AKShare 旧适配器纳入同一归一化口径；用 `SH600519` 创建的笔记、用 `600519.SH` 创建的提醒，后续都按 `600519` 查询和展示。
   - 2026-07-15 系统备份导入预检和导入执行也会归一化自选股代码，跨设备恢复时 `SH600519` / `600519.SH` 会落库为 `600519`，并继续去重和忽略未知标的。
   - 2026-07-15 旧备份或旧状态里的笔记、价位提醒如果已经保存了市场前后缀，读取列表时会自动改成 6 位代码并回写状态文件，后续导出也会使用规范代码。
   - 2026-07-15 旧诊断报告里的 `diagnosis.symbol` 和 `data_quality.symbol` 也会在报告列表读取时自动归一化并回写，诊断历史对比会用统一 6 位代码匹配历史记录。
   - 2026-07-15 Tushare 预检、组合风险模拟权重和真实持仓输入也支持市场前后缀代码，例如 `SH600519`、`600519.SH`、`300750.SZ`。
   - 2026-07-15 前端本地组合输入也统一归一化：localStorage 恢复、持仓 CSV、交易流水 CSV 导入都会把市场前后缀代码保存为 6 位代码，报告导出和后端请求不再带旧格式。
   - 2026-07-15 前端已抽出共享 `normalizeAShareSymbol()`，页面输入和 API 客户端共用同一口径；即便后续新增入口直接调用 `fetchPortfolioRisk()` 或 `fetchTushareProbe()`，也会在拼接 URL 前把 `SH600519`、`000001.SZ` 等格式转为 6 位代码。

4. 数据可信度面板增强
   - `/api/v1/system/data-connectors` 增加东方财富、腾讯行情、新浪资金流、通达信本地日线、同花顺本地股票名表、AKShare、Mock、Tushare Pro 的状态展示。
   - 前端将内部来源标记转换为中文说明，例如“新浪资金流兜底”“腾讯估值兜底”“通达信本地 K 线”。
   - 面板继续展示请求超时、缓存 TTL、缓存命中、fallback 状态和最近刷新记录。

5. 策略回测数据血缘
   - 回测服务优先使用真实历史 K 线。
   - 回测报告记录价格来源、历史样本数量、最后交易日和 fallback 原因。
   - 样本可信度、稳定性、收益/回撤、权益曲线、历史对比等现有能力继续保留。
   - 回测已支持可配置止盈/止损退出规则，价格路径先触发阈值会提前退出，否则持有到期；前端参数、历史记录、动作状态 key 和报告导出均保留该口径。
   - 回测已增加“跌破 MA20 退出”开关，开启后持有窗口内若收盘价先跌破 MA20 会提前退出，并在交易明细中标记“跌破 MA20”。
   - 回测已增加“量比低于阈值退出”参数，支持用缩量作为突破失效条件；默认 0 关闭，开启后交易明细标记“缩量退出”。
   - 最近回测列表和 HTML/Markdown 报告已显示每次回测的退出参数口径，例如固定持有、止盈/止损、MA20 或量比阈值，便于跨参数组合复盘。
   - 回测报告已增加退出原因分布，前端面板和 HTML/Markdown 报告可汇总持有到期、止盈、止损、跌破 MA20、缩量退出、诊断转弱的笔数。
   - 回测已增加“诊断分低于阈值退出”参数，默认 0 关闭；开启后用价格、MA20 偏离和量比估算持有窗口内的诊断代理分，跌破阈值会标记“诊断转弱”并提前退出。
   - 诊断代理分退出已补充触发/未触发说明，交易样本、HTML 报告和 Markdown 报告会显示代理分与阈值判断，方便复核退出依据。
   - 回测复盘动作已能识别“诊断转弱”退出，自动提示复核阈值是否过紧，指标中带出最低代理分，并检查对应标的技术面和资金面变化。
   - 回测历史摘要已保存退出原因分布、诊断转弱笔数和最低代理分；前端最近回测和 HTML/Markdown 报告可展示历史诊断转弱上下文。
   - 回测历史对比已增加“诊断转弱变化”指标，便于观察调参后诊断转弱退出是否增加或减少。
   - 回测复盘动作会在诊断转弱退出较上一轮增加时生成“复核诊断转弱增加”事项，把历史变化直接转成跟踪动作。
   - 2026-07-15 回测“诊断分低于阈值退出”已优先读取已保存诊断报告快照作为基线，用历史快照总分校准持有路径代理分；无快照时继续安全回退到原代理分口径，交易明细会带出来源、基线分和基线日期。
   - 2026-07-15 修复回测历史 ID 偶发碰撞：同一策略、同一周期、同一时间戳连续记录两次时不再覆盖上一条，历史对比和复盘动作不会丢样本。

6. 组合风险真实化
   - 自定义仓位权重低于 95% 时，组合风险建议会提示现金缓冲；高于 105% 时提示核对杠杆或重复录入。
   - 组合风险支持输入组合市值，自动估算每个模拟仓位金额和现金缓冲金额。
   - 行业暴露已加入集中度等级、建议上限、超额权重和超额金额，避免现金缓冲场景下误判行业暴露口径。
   - 前端会在本地保存组合市值和模拟仓位输入，刷新页面后自动恢复并重新请求组合风险。
   - 组合风险新增真实持仓输入，支持数量、成本价、市值反推仓位、成本金额、浮盈亏和收益率展示。
   - 组合风险 URL 参数里的模拟权重和真实持仓会统一归一化股票代码，带市场前后缀的输入也能匹配到当前自选股。
   - 前端本地组合输入、持仓导入和交易流水导入也会统一归一化代码，避免页面输入、localStorage 和报告 payload 出现 `SH600519` / `600519.SH` 等旧格式。
   - 前端 API 客户端已补上同样的归一化兜底，未来其他组件绕过当前表单直接请求组合风险时，URL 参数仍会使用规范 6 位代码。
   - 前端支持从 CSV/TXT 导入持仓，格式可用 `symbol,shares,cost_price`，导入后自动填充真实持仓输入。
   - 前端支持从 CSV/TXT 导入交易流水，格式可用 `symbol,side,shares,price`，按买入加权成本、卖出平均成本扣减生成当前持仓。
   - 持仓和交易流水导入已支持常见券商中文表头，例如证券代码、持仓数量、成本价、买卖方向、成交数量、成交价格。
   - 持仓导入继续兼容证券余额、股票余额、可用数量、摊薄成本价、参考成本价等表头；交易流水继续兼容买卖标志、成交股数、成交均价以及买入成交/卖出成交等方向。
   - 券商 CSV/TXT 导入已支持双引号字段、千分位逗号和人民币符号，避免 `"1,008.50"` 这类成本价被拆列或判为无效。
   - 前端组合风险面板和导出报告继续复用 `suggestions` 展示这些持仓口径提示，并在报告中保留持仓输入与导入格式说明，方便复现组合口径。

7. 报告与测试
   - README 已补充真实数据 provider、通达信、同花顺和新浪资金流说明。
   - 后端测试补充东方财富、腾讯、通达信、同花顺、新浪资金流路径。
   - 前端测试覆盖数据可信度、导出报告和相关交互。
   - 系统备份/恢复已覆盖自选、诊断报告、笔记、价位提醒、行动状态和策略回测历史，跨设备恢复不再遗漏回测记录。
   - 系统备份导入预检结果可一键取消，选错备份文件时无需刷新页面即可清理待导入状态。
   - JSON/HTML/Markdown 研究报告已写入连接器明细、缓存遥测、数据质量检查、运行配置和本地路径状态。
   - HTML/Markdown 研究报告已增加固定“结论页”和“固定风险提示”，便于直接归档或给公司内部复盘使用。
   - HTML 研究报告已增加封面视觉和打印分页样式，并在工具栏提供“打印/PDF”入口，可直接调起浏览器打印为 PDF。
   - HTML/Markdown 报告展示层会把周期显示为短线/波段/中线，避免把 `intraday/swing/position` 内部枚举暴露给报告读者。
   - 数据质量接口已纳入运行环境检查，会结合连接器 fallback、缺包/异常、缓存过期和缓存命中率调整质量分与状态。
   - 个股数据质量面板已细分 warn 文案：来源覆盖显示“部分兜底”，运行环境问题显示“运行需刷新/需刷新”。
   - 数据质量总览已支持按全部、运行需刷新、部分兜底、需核验、异常筛选，便于快速定位问题标的。
   - 数据质量总览接口已返回运行环境、来源兜底、普通核验三类 warning 计数，前端筛选标签优先使用后端统计，避免不同客户端口径不一致。
   - 保存诊断报告时会同步归档当次数据质量快照，报告历史可显示保存时的数据质量分与状态，旧报告继续兼容。
   - 报告历史已支持导出单条已保存报告归档 JSON/HTML/Markdown，包含保存时的诊断、周期、生成时间和数据质量快照。
   - 已保存报告归档也可从报告历史直接打开可打印 HTML，便于浏览器另存 PDF。
   - 已保存报告的 HTML/Markdown 归档会显式显示原始报告生成时间，避免与导出时间混淆。
   - 报告历史删除已加入行级进行中保护，只禁用当前删除按钮并显示“删除中”无障碍状态，避免重复删除请求。
   - 报告历史会显示已保存报告的周期；点击已保存报告会同时恢复保存时的标的和周期，避免跨短线/波段/中线复盘时口径错位。
   - 诊断变化趋势点已带上历史保存报告和本次实时诊断的数据质量分/状态，复盘评分变化时可同时看到数据可信度。

8. 运行配置可视化
   - 新增 `/api/v1/system/runtime-config` 只读接口，返回当前 provider、请求超时、缓存 TTL、数据新鲜度阈值和本地路径配置状态。
   - 前端系统区新增“运行配置”面板，可直接看到通达信 `vipdoc`、同花顺股票名表是否已配置且存在。
   - 面板明确提示修改运行参数需要调整后端环境变量并重启服务。
   - 系统就绪度已纳入运行配置检查，会提示当前 provider、本地路径存在性和 Tushare Token 配置状态，不返回 token 明文。
   - 新增 Tushare Pro Token 配置状态展示，只返回是否配置，不返回 token 明文；连接器健康会提示缺包、缺 token 或已具备接入前置条件。
   - `STOCK_DOCTOR_DATA_PROVIDER=tushare` 已可安全启动；在财务字段归一化完成前会委托 Mock 回退，并在连接器健康中标明。
   - Tushare provider 第一阶段已支持在包/token 可用时归一化 PE/PB/ROE/营收增速/利润增速，并把来源写入数据质量报告；失败路径继续安全回退。
   - Tushare provider 已在 token 可用时尝试读取前复权日线，回测可优先使用真实复权价格路径，空数据或异常时仍回退 Mock 历史。
   - Tushare provider 已在 token 可用时尝试读取基础资料，成功后会更新快照名称/行业，并在数据质量报告中显示“Tushare 基础资料”。
   - Tushare 财务指标已扩展到 EPS、毛利率、净利率、资产负债率、每股经营现金流、现金流利润比、流动比率、速动比率、总资产周转率、销售/管理/财务费用率和权益乘数；诊断证据会在字段存在时解释盈利空间、费用压力、资产效率、财务杠杆、利润现金含量和短期偿债缓冲，数据质量会校验异常比例。
   - Tushare 调用失败时会记录最近失败步骤，并在连接器健康中显示 fallback 原因；token 不会出现在接口响应或日志文案里。
   - 新增 `/api/v1/system/tushare-probe` 只读预检接口，按步骤检查 tushare 包、Token、Pro API、基础资料、日行情基础指标、财务指标和前复权日线。
   - 运行配置面板新增“检测 Tushare”按钮，可直接看到缺包、缺 Token、接口返回空数据或调用失败的下一步建议；预检不会切换当前 provider，也不会返回 Token 明文。
   - 东方财富 K 线接口返回 `data: null` 或无可用 K 线时，已改为自动走腾讯 K 线/通达信本地日线兜底，避免页面初始化接口被空数据拖成 500。
   - 后端声明 `real-data` 可选依赖组，便于后续用 `pip install -e ".[real-data]"` 一次安装 AKShare/Tushare 真实数据包。
   - 本机后端虚拟环境已安装 `real-data` 可选依赖，当前 Tushare 预检已从“缺包”推进到“包可导入，等待 Token”。
   - JSON/HTML/Markdown 研究报告已加入 Tushare 预检上下文；已执行预检时导出状态、步骤和下一步，未执行时明确写“本次导出未执行只读预检”。
   - 新增 `backend/.env.example`，集中记录 eastmoney、通达信、同花顺、Tushare Token、缓存 TTL、请求超时和新鲜度阈值配置模板。
   - Tushare 预检结果已增加总耗时、步骤耗时和接口返回行数；未配置 Token 时展示包/Token 检查耗时，配置 Token 后可进一步观察各 Tushare 接口是否慢、空数据或权限不足。
   - 本机已完成 Tushare Token 本地 `.env` 配置验证；接口不返回 token 明文，Git 不跟踪该配置文件。
   - 当前 Tushare 真实预检为“部分通过”：包、Token、Pro API 初始化正常；未复权 `daily` 日线可返回 5960 行样本；`stock_basic`、`daily_basic` 和 `adj_factor` 受账号频控限制，`fina_indicator` 当前账号无访问权限。
   - `pro_bar` 已补充 `set_token` 调用，并在前复权因子受限时自动降级到未复权 `daily` 日线；数据可信度面板会以 warn 展示“部分真实数据可用”，而不是误判为整体失败。
   - 通达信本地日线适配器已支持自动发现常见目录下的 `vipdoc`，兼容 `sh600519.day` 和 `SH600519.day` 文件名。
   - 若配置路径不存在但发现其他 `vipdoc`，运行配置会显示“实际使用”路径；若样本日线过期，会标记为 fallback/已过期，并禁止用旧 K 线参与诊断或回测兜底。
   - 本机当前可发现 `E:\股票\渤海证券行情加交易\new_bhzq_v6\Vipdoc`，但样本最新交易日为 2013-03-21，已判定过期；需要在通达信客户端确认最新下载位置或重新补全日线。
   - 新增 `/api/v1/system/tdx-probe` 只读检测接口和前端“检测通达信”按钮，可列出候选 `vipdoc`、是否当前使用、样本覆盖、行数、最新交易日和过期状态。
   - JSON/HTML/Markdown 研究报告已加入通达信预检上下文；已执行检测时导出候选路径、实际使用路径、最新交易日和过期状态，未执行时明确写“本次导出未执行通达信只读检测”。
   - 2026-07-15 新增后端自动刷新调度骨架，默认关闭；可通过 `STOCK_DOCTOR_DATA_AUTO_REFRESH_ENABLED`、`STOCK_DOCTOR_DATA_AUTO_REFRESH_INTERVAL_MINUTES`、`STOCK_DOCTOR_DATA_AUTO_REFRESH_SCOPE` 和 `STOCK_DOCTOR_DATA_AUTO_REFRESH_ON_STARTUP` 启用。启用后 FastAPI 生命周期会按配置触发刷新任务，并继续复用现有刷新历史和数据新鲜度面板。
   - 运行配置面板和 `/api/v1/system/runtime-config` 已显示自动刷新是否开启、刷新范围、间隔和是否启动即刷新；本机自检脚本会在后端重启后同步输出该配置。
   - HTML/Markdown 研究报告的数据可信度章节已显示自动刷新配置，离线报告可审计当前是否启用、刷新范围、间隔和启动刷新策略。
   - 2026-07-15 继续增强自动刷新运行态：调度器会记录是否正在运行、启动时间、下次计划刷新时间、最近一次调度开始/结束时间、成功/失败状态、失败原因和调度次数。
   - `/api/v1/system/runtime-config`、运行配置面板、本机自检脚本、系统就绪度和 HTML/Markdown 报告均已纳入上述运行态，避免只看到“已开启配置”却不知道后端调度器是否真正启动。
   - 2026-07-15 继续把自动刷新运行态下沉到 `/api/v1/system/data-connectors` 和“数据可信度”面板；诊断时无需切到运行配置面板，也能看到自动刷新是否启用/运行、下次计划刷新和最近调度结果。

## 本机验证结果

- 后端全量测试：`188 passed, 1 warning`（测试时临时使用 `STOCK_DOCTOR_DATA_PROVIDER=mock`，避免本机真实 provider 网络波动影响单元测试）
- 前端测试：`57 passed`
- 前端生产构建：通过
- 接口验证：`/api/v1/system/tushare-probe?symbol=600519` 返回 `warn`；Token 已配置，未复权日线可用，财务指标权限和频控限制已在步骤详情中展示。
- 接口验证：`/api/v1/system/runtime-config` 可返回通达信配置路径、自动发现路径和过期说明；`/api/v1/system/data-connectors` 对过期通达信日线返回 fallback。
- 接口验证：`/api/v1/system/tdx-probe` 当前返回 `warn`，列出 5 个候选路径；当前解析路径最新交易日 2013-03-21，已标记过期。
- 2026-07-15 新增本机交付自检脚本：`powershell -ExecutionPolicy Bypass -File .\scripts\check-local.ps1`，可只读检查目录、Python/Node/npm、依赖、端口、健康接口、系统就绪度、运行配置和前端页面；当前自检结果为 `0 failure(s), 1 warning(s)`，警告来自运行配置仍使用 mock/本地路径类提示。
- 2026-07-15 自动刷新调度验证：后端全量测试更新为 `188 passed, 1 warning`，前端测试仍为 `55 passed`，前端生产构建通过；当前已运行的旧后端进程需重启后才会在自检输出里显示 `auto_refresh` 新字段和调度运行态；数据可信度面板已同步展示自动刷新运行态。
- 2026-07-15 报告导出补充验证：HTML/Markdown 报告已纳入自动刷新配置；前端测试 `55 passed`。
- 2026-07-15 历史诊断快照基线回测验证：后端回测/API 定向测试 `19 passed, 1 warning`，前端测试 `55 passed`。
- 2026-07-15 前端共享股票代码归一化验证：API 客户端单测覆盖组合风险权重/持仓和 Tushare 预检参数，前端测试更新为 `57 passed`，前端生产构建通过。

## 本地运行配置

后端 `.env` 建议：

```env
STOCK_DOCTOR_DATA_PROVIDER=eastmoney
STOCK_DOCTOR_DATA_REQUEST_TIMEOUT_SECONDS=8
STOCK_DOCTOR_DATA_CACHE_TTL_SECONDS=300
STOCK_DOCTOR_DATA_FRESHNESS_STALE_AFTER_MINUTES=30
STOCK_DOCTOR_DATA_AUTO_REFRESH_ENABLED=false
STOCK_DOCTOR_DATA_AUTO_REFRESH_INTERVAL_MINUTES=240
STOCK_DOCTOR_DATA_AUTO_REFRESH_SCOPE=watchlist
STOCK_DOCTOR_DATA_AUTO_REFRESH_ON_STARTUP=false
STOCK_DOCTOR_TDX_VIPDOC_PATH=E:\new_tdx64\vipdoc
STOCK_DOCTOR_THS_STOCKNAME_PATHS=D:\同花顺软件\同花顺\stockname\stockname_16_0.txt;D:\同花顺软件\同花顺\stockname\stockname_32_0.txt
STOCK_DOCTOR_TUSHARE_TOKEN=<如需 Tushare 财务增强，填入 token>
```

后端启动：

```powershell
cd backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8010
```

如果已在 `backend` 下创建虚拟环境，也可以用 `.\.venv\Scripts\python -m uvicorn app.main:app --host 127.0.0.1 --port 8010`。

前端启动：

```powershell
cd frontend
npm run dev -- --host 127.0.0.1 --port 30080
```

交付或换电脑前建议先执行本机自检：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\check-local.ps1
```

这些值会通过 `/api/v1/system/data-connectors` 和 `/api/v1/system/runtime-config` 返回，并显示在数据可信度/运行配置面板中。报告导出的 JSON、HTML、Markdown 也会保留这些运行假设，便于离线复盘。

## 明天继续开发建议

1. 财务数据第二阶段
   - 已完成 Tushare Pro 包/token 准备度展示、安全 provider 回退、基础资料、基础财务指标、EPS、毛利率、资产负债率、经营现金流、流动性比率、费用率、权益乘数和前复权日线读取。
   - 已补充运行配置面板内的一键只读预检，并已在本机安装真实数据依赖；下一步配置 token 后，直接用预检确认接口权限，再扩展现金流量表、资产负债表更多细分字段。

2. 真实数据质量评分
   - 已把来源覆盖率、更新时间、fallback 次数、缓存过期和缓存命中率纳入股票级质量评分。
   - 已细分“真实可用”“部分兜底”“运行需刷新”的前端展示文案，并加入质量总览筛选器。
   - 后端已具备默认关闭的自动刷新调度；下一步可在真实 provider 稳定后开启 `watchlist` 范围定时刷新，并观察刷新失败率、覆盖率和接口频控。

3. 组合风险和回测真实化
   - 组合风险已加入现金缓冲/超额权重提示、模拟市值/仓位金额估算、行业集中度阈值、真实持仓成本、浮盈亏、持仓导入、交易流水导入、更多券商中文表头适配和带千分位/引号的复杂 CSV 解析。
   - 回测策略已增加止盈/止损、MA20 跌破、量比低于阈值和诊断转弱退出规则，并保存每次参数组合；诊断转弱退出已能使用已保存诊断快照校准路径分，下一步可继续把历史快照扩展为更完整的买卖规则和按日期回放。

4. 报告导出增强
   - 已把真实数据来源、通达信校验、同花顺解析、资金流 fallback、数据质量检查和运行配置写入报告“数据可信度”章节。
   - 已增加公司复盘可用的结论页、固定风险提示模板、HTML 封面视觉和打印分页样式；后续可继续增加真正的服务端 PDF 生成。

5. GitHub 协作
   - 到公司后先执行 `git fetch --all` 和 `git pull --ff-only origin local-codex-progress`。
   - 若公司也改了同一分支，先看 `git log --oneline --decorate --graph --all -20`，再决定 merge/rebase。
